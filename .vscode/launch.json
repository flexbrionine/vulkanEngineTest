{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Windows launch",
            "program": "${workspaceFolder}/win32_platform.exe",
            "type": "cppvsdbg",
            "request": "launch",
            "cwd": "${workspaceFolder}"
        }
    ]
}