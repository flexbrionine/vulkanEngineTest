{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Windows build",
            "command": "${workspaceFolder}/build.bat",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}